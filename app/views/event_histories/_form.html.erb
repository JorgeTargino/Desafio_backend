<%= form_with(model: event_history, local: true) do |form| %>
  <% if event_history.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(event_history.errors.count, "error") %> prohibited this event_history from being saved:</h2>

      <ul>
      <% event_history.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :subscription_id %>
    <%= form.text_field :subscription_id %>
  </div>

  <div class="field">
    <%= form.label :type %>
    <%= form.text_field :type %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
